import {
  borrarSlider,
  crearSlider,
  modificarSlider,
  obtenerSliders,
} from "../services/sliders.services.js";

import { Request, Response } from "express";

const obtenerTodosLosSliders = (req: Request, res: Response) => {
  try {
  } catch (error) {}
};
